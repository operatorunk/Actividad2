install podman

wget https://mirror.stream.centos.org/9-stream/AppStream/x86_64/os/Packages/podman-4.6.1-1.el9.x86_64.rpm

sudo dnf install ./podman-4.6.1-1.el9.x86_64.rpm -y

sudo dnf install -y

cd /tmp
wget https://mirror.stream.centos.org/9-stream/AppStream/x86_64/os/Packages/podman-*.rpm
sudo dnf localinstall podman-*.rpm -y

podman --version

podman info
If running as non-root, it should show:

rootless: true
